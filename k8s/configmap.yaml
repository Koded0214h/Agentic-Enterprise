apiVersion: v1
kind: ConfigMap
metadata:
  name: aos-config
  namespace: aos
data:
  DEBUG: "0"
  ALLOWED_HOSTS: "aos.yourdomain.com localhost"
  CORS_ALLOWED_ORIGINS: "https://app.yourdomain.com"
  CELERY_BROKER_URL: "redis://aos-redis:6379/0"
  VECTOR_STORE_PATH: "/app/chroma_db"
